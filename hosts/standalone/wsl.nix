# WSL/Standalone Linux host configuration
# For non-NixOS Linux systems using standalone home-manager

{ ... }:

{
  # Host-specific customizations for WSL/standalone Linux
  # Most configuration comes from shared modules
}